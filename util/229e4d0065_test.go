// ********RoostGPT********
// Test generated by RoostGPT for test go-calculator using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
package util

import (
    "testing"
    "unicode"
)

func TestRandomStringPositive(t *testing.T) {
    length := 10
    str := RandomString(length)
    if len(str) != length {
        t.Errorf("RandomString(%d) returned a string of incorrect length: got %d, want %d", length, len(str), length)
    }
    for _, char := range str {
        if !unicode.IsLetter(char) {
            t.Errorf("RandomString(%d) returned a string with non-letter character: got %v", length, str)
            break
        }
    }
}

func TestRandomStringNegative(t *testing.T) {
    length := -1
    str := RandomString(length)
    if len(str) != 0 {
        t.Errorf("RandomString(%d) should return an empty string for negative input: got %v", length, str)
    }
}

